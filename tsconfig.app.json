{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    "types": ["vite/client"],
    "allowArbitraryExtensions": true,
    "skipLibCheck": true,

    /* src/components/grok-bot/vendor/ 下是原样引入的上游 .js（IIFE），
       不开 allowJs 这些副作用 import 会报 TS2307 找不到模块。
       配套的全局类型在 grok-bot/grok-character.d.ts。 */
    "allowJs": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* 路径别名：@/ → src/
       须与 vite.config.ts 的 resolve.alias 保持一致。
       paths 自 TS 4.4 起可独立解析，不使用 baseUrl（其将在 TS 7 移除）。 */
    "paths": {
      "@/*": ["./src/*"]
    },

    /* Linting */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
